services:
  - type: web
    name: phishing-detector-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0  # ← CHANGE FROM 3.9.0 to 3.11.0
